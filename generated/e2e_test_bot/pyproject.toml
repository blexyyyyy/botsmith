[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "e2e_test_bot"
version = "0.1.0"
description = "A bot created with BotSmith"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests",
]

[project.scripts]
e2e_test_bot = "e2e_test_bot.main:main"
